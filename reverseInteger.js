/**
 * @param {number} x
 * @return {number}
 */

// function to reverse digits of an integer
var reverse = function (x) {
  // take mod to figure out how many places it has
  // 123 -> 321, 300 + 20 + 1
  // 100 -> 1, 0 + 0 + 1
  // if number is negative
};
